services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    ports:
      - "2091:2091"
    env_file:
      - .env
    volumes:
      - ./server:/app/server
      - /app/node_modules
    restart: always
