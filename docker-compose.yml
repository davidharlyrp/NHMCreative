services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    ports:
      - "3001:3001"
    env_file:
      - .env
    volumes:
      - ./server:/app/server
      - /app/node_modules
    restart: always
